# Bounds Sandbox

This repository is a sandbox for testing out bound computation for lidar-like registration.

## Dependencies

The dependencies are listed in `requirements.txt`. You can install them using pip:

```bash
pip install -r requirements.txt
```

## Usage

No much for the moment, but you can run the main script to see the simulated data:

```bash
python bounds_sandbox.py
```